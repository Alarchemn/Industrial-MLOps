from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning to detect machine failures (End-to-End MLops project)',
    author='Martin Alarcon',
    license='MIT',
)
